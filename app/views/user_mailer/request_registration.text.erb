<%= t('application.name') %>
----------

<%= t('mailers.user_mailer.request_registration.dear', last_name: @temp_user.last_name, first_name: @temp_user.first_name) %>

<%= t('mailers.user_mailer.request_registration.message_text', expired_at: l(@temp_user.expired_at, format: :default)) %>

<%= url_for(controller: 'account', action: 'regist', locale: @locale, token: @temp_user.token ) %>

----------
<%= t('mailers.mailer.footer') %>